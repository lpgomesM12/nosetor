<%= form_for(@st_item) do |f| %>
  <% if @st_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@st_item.errors.count, "error") %> prohibited this st_item from being saved:</h2>

      <ul>
      <% @st_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="row">
    <div class="col-lg-6">
    <%= f.label :nome_item %><br>
    <%= f.text_field :nome_item, :class => 'form-control' %>
   </div>
</div>

<br/>

<div class="actions">
    <%= f.submit %>
  </div>
<% end %>
