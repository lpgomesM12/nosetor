<%= form_for(@st_item) do |f| %>
  <% if @st_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@st_item.errors.count, "error") %> prohibited this st_item from being saved:</h2>

      <ul>
      <% @st_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome_produto %><br>
    <%= f.text_field :nome_produto %>
  </div>
  <div class="field">
    <%= f.label :st_categoriaproduto_id %><br>
    <%= f.text_field :st_categoriaproduto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
